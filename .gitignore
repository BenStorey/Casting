target/
**/*.rs.bk
*.db
*.db-wal
*.db-shm
.casting/
.tmp-demo/

# Frontend build artifacts (node deps + compiled SPA output)
frontend/node_modules/
frontend/dist/

# Local dev workspace. NOTE: the D5 ownership boundary requires the `cast run`
# workspace OUTSIDE the source tree (see /home/ben/casting-workspace/ and
# docs/DEPLOYMENT.md). `.dev/` is ignored defensively only, in case a stale
# in-tree dir exists; it is not the active dev workspace anymore.
.dev/
